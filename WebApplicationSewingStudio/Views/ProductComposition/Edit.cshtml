@model WebApplicationSewingStudio.ViewModels.ProductCompositionsViewModels.ProductCompositionsViewModel

@{
    ViewData["Title"] = "Редактирование состава изделия";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Редактирование</h2>

<h4>Состав изделия</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ProductCompositions.First().Id" />
            <div class="form-group">
                <label asp-for="ProductCompositions.First().Product.Name" class="control-label"></label>
                <select asp-for="ProductCompositions.First().Product.Name" class="form-control" asp-items="Model.ProductsList"></select>
                <span asp-validation-for="ProductCompositions.First().Product.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ProductCompositions.First().Material.Name" class="control-label"></label>
                <select asp-for="ProductCompositions.First().Material.Name" class="form-control" asp-items="Model.MaterialsList"></select>
                <span asp-validation-for="ProductCompositions.First().Material.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ProductCompositions.First().Quantity" class="control-label"></label>
                <input asp-for="ProductCompositions.First().Quantity" class="form-control" />
                <span asp-validation-for="ProductCompositions.First().Quantity" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Сохранить" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Вернуться назад</a>
</div>

